# TheMealDB API Configuration (Proxied via Next.js Rewrites)
# Set to relative path to avoid CORS issues in local development and production.
NEXT_PUBLIC_MEALDB_API_URL=/api/mealdb
NEXT_PUBLIC_MEALDB_IMAGE_URL=/images/mealdb
